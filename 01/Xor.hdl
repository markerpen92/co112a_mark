CHIP Xor{
    IN a;
    IN b;
    OUT out;

    PARTS : 
        Not(in=b , out=notb)
        Not(in=a , out=nota)
        Nand(in=a , in=notb , out=out1)
        Nand(in=nota , in=b , out=out2)
        
}